FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl git iptables procps ca-certificates nodejs npm build-essential \
    && rm -rf /var/lib/apt/lists/* \
    && useradd -m -s /bin/bash zoo

WORKDIR /home/zoo
CMD ["sleep", "infinity"]
