[project]
name = "zoo-sdk"
version = "0.1.0"
description = "Python client for zoo sandboxes"
requires-python = ">=3.10"
dependencies = ["requests>=2.31"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["zoo_sdk"]
